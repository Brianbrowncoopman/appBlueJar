import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PedidobodegaPage } from './pedidobodega.page';

describe('PedidobodegaPage', () => {
  let component: PedidobodegaPage;
  let fixture: ComponentFixture<PedidobodegaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PedidobodegaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
